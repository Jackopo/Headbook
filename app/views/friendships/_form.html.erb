<%= form_for(@friendship) do |f| %>
  <% if @friendship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friendship.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
      <% @friendship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_person %><br />
    <%= f.number_field :first_person %>
  </div>
  <div class="field">
    <%= f.label :second_person %><br />
    <%= f.number_field :second_person %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
